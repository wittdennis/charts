apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yas3p.fullname" . }}-test-connection"
  labels:
    {{- include "yas3p.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yas3p.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
