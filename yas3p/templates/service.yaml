apiVersion: v1
kind: Service
metadata:
  name: {{ include "yas3p.fullname" . }}
  labels:
    {{- include "yas3p.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yas3p.selectorLabels" . | nindent 4 }}
