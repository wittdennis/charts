apiVersion: v1
kind: Service
metadata:
  name: {{ include "home-assistant-otbr.fullname" . }}-web
  labels:
    {{- include "home-assistant-otbr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.web.type }}
  ports:
    - port: {{ .Values.service.web.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "home-assistant-otbr.selectorLabels" . | nindent 4 }}
